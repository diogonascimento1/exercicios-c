#include<stdio.h>
#include<string.h>
struct Palavras{
    char palavra;
    int rep;
}
int main(){
    fgets(palavra,palavra[50],stdin);
    for (int i=0,i<=50,i++){
        if(strcpy  (Palavras[i].palavra,Palavras[i].palavra+1);){
            Palavra.rep=Palavra.rep+1
           
        }
printf(%s%d;"as repetiçoes de cada palavra são respectivamente,"Palavra.palavra[i];Palavra.rep[i]"")
    }
}
return 0;
